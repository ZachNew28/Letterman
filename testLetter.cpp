#include "letter.h"

int main(int argc, char* argv[]) {
    // See if it works correctly on a simple dictionary
    Letterman test1;

    test1.getOptions(argc, argv);


    // See if it works correctly on a complex dictionary
    Letterman test2;


    // See if it works on a complex dictionary that has special characters at the very end of the word
    Letterman test3;
}
